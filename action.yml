name: 'create-env-json'
description: 'Create an environment file in JSON format'
inputs:
  file-name:
    description: 'The name of the file to be written'
    required: true
  custom-inputs:
    description: 'pass the key value pairs in this format key=value'
    required: true
outputs:
  full-path:
    description: 'The full path to the written file'
runs:
  using: 'node20'
  main: 'index.js'
branding:
  icon: 'book-open'  
  color: 'yellow'
